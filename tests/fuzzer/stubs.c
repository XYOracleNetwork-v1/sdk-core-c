#include "../../src/XYResult.h"
#include "../../src/network.h"
XYResult_t socket_send(NetworkPipe_t* self, char* data, uint32_t count, uint8_t debug){
  DECLARE_RESULT()
  return result;
}
XYResult_t socket_recv(NetworkPipe_t* self, char* data, uint32_t recvBytes){
  DECLARE_RESULT()
  return result;
}
